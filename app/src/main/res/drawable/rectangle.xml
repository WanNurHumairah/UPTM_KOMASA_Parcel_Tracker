<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/brown" /> <!-- Background color of the button -->
    <corners android:radius="20dp" /> <!-- Corner radius -->
    <stroke
        android:width="2dp"
        android:color="@color/white" /> <!-- Border width and color -->
</shape>
